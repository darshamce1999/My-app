import CircularProgress from '@mui/material/CircularProgress';

export const CircularProgressComp = () => {
    return <CircularProgress color='warning' size={50} thickness={5} value={50} variant="indeterminate"/>
}